# S1--S11 concrete certificate obligations

The fixed maximum stable sets are `x1={3,69,88,154,223}`, `x2={4,51,121,189,247}`, and `x3={67,119,166,189,247}`. `x1+x2` and `x1+x3` are the two compatible two-top packings. Every “exact top” condition means the selected size-five classes are precisely the named packing; lower-scored residual columns are reconstructed exhaustively from the remaining vertices.

The normative compact mapping is `S1_S11.tsv`. All targets in that file are strict exclusion targets: a verified maximum score `< target` yields the displayed integer-coordinate consequence. S1 and S2 instead use exact rational floor bounds. The integrated entry point is `integrated_checker/scripts/exact_mscp_conditional_verify.py`; the second entry point is `independent_checker/scripts/verify.py` after graph staging.

Column counts for S4 and S7--S11 are respectively 178, 2,858, 2,880, 2,550, 5,953, and 5,500. S3, S5, and S6 reconstruct 160, 183, and 138 residual quadruples. The denominators/right-hand numerators of the rational obligations are checked from payloads rather than repeated as prose defaults: S1 `(D,rhs)=(26,1039)`, S2 `(2,73)`, S3 `(3,109)`, S5 `(25,937)`, and S6 `(2,67)`.

Sharpness witnesses are located in `integrated_checker/instances/dsjc2509/conditional/witnesses/primal_witnesses.json` and `independent_checker/witnesses/witnesses.json`. The S11/H2-F witness has profile `(3,4,31,34,2)` and objective 8,278; it establishes envelope sharpness and is not the 8,277 incumbent.
